BEGIN PHASE 6 — PART 2 (UI WIRING)

The challengeStore.js and eventsStore.js cloud functions are now implemented. 
Now wire ALL relevant UI components to these cloud functions.

Here is the full scope:

-----------------------------------------
1. CHALLENGES (FRIEND VS FRIEND)
-----------------------------------------

Files to update:
- src/pages/Challenges.jsx
- src/pages/ChallengeGame.jsx
- src/components/ChallengeResultModal.jsx (if exists)
- src/components/FriendChallengeCard.jsx (if exists)

Tasks:
- Replace all local challenge creation with createChallengeCloud()
- Replace all local attempt saving with submitChallengeAttempt()
- Replace all local loading with:
    loadPendingChallenges()
    loadActiveChallenges()
    loadChallengeHistory()
- Ensure completed challenges load correctly
- Remove any remaining simulated/AI challenge code

Buttons to test and ensure fully functional:
- “Start Challenge”
- “Play Now”
- “Send Challenge”
- “View Results”

-----------------------------------------
2. BOSS LEVEL (DAILY CHALLENGE)
-----------------------------------------

Files to update:
- src/pages/BossLevel.jsx

Tasks:
- Replace all local boss attempt storage with:
    saveBossAttemptCloud()
    loadBossAttemptToday()
    canPlayBossTodayCloud()

Buttons to test and ensure functional:
- “Start Boss Level”
- “Try Again Tomorrow”
- Result modal

-----------------------------------------
3. GLOBAL EVENTS (WEEKLY EVENTS)
-----------------------------------------

Files to update:
- src/pages/GlobalEvents.jsx
- src/pages/EventQuiz.jsx
- src/components/EventResultModal.jsx (if exists)

Tasks:
- Replace all local event logic with cloud versions:
    enterEventCloud()
    hasEnteredCloud()
    loadLeaderboard()
    getMyRank()
    claimEventRewards()
- Sync event results to cloud after quiz completion
- Show provisional rank and final rank from Supabase

Buttons to test and ensure fully functional:
- “Enter Event”
- “Play Event”
- “Submit”
- “Claim Rewards”
- Leaderboard tab switching

-----------------------------------------
4. VALIDATION & CLEANUP
-----------------------------------------

- Remove ANY leftover references to localStorage challenge/event data
- Ensure no simulated/AI logic remains anywhere
- Ensure no unused imports or dead code
- Make sure all UI interactions call the correct cloud store functions
- Ensure each screen loads cloud data correctly on mount
- Ensure navigation after challenge/game/event completes works normally

-----------------------------------------
5. QUALITY CHECK
-----------------------------------------

After wiring, run the app and verify the following:
- Every button in Challenges, Boss Level, and Global Events works
- Challenges can be created, played, and results shown correctly
- Boss Level can be played once per day and syncs to cloud
- Global Events can be entered, played, and rewards claimed
- No console errors
- No 406 or 404 Supabase errors
- No UI freezes or null states

Stop after completing UI wiring and verifying all buttons and flows work. 
Wait for confirmation before moving to sync engine work in Phase 7.

Do a full production hardening pass based on this audit. Fix EVERYTHING listed. Do not skip items.

A) friendsStore.js — remove RLS-broken legacy refresh + dead code
1) Find these legacy functions in src/store/friendsStore.js:
   - loadFriends()
   - loadRequests()
   - loadPendingRequests()
   They use profiles .in(ids) and break under RLS.

2) Replace every callsite that uses those legacy functions AFTER mutations with loadAll() only.
   Specifically: after these actions complete successfully, call ONLY await loadAll():
   - acceptFriendRequest
   - declineFriendRequest
   - cancelSentRequest
   - removeFriend
   - sendFriendRequest
   Then delete the legacy functions completely (or convert them into wrappers that call loadAll but DO NOT query profiles).

3) Ensure loadAll() is the only source of truth for:
   friends, sentRequests, receivedRequests, users (profile map)

B) eventsStore.js — remove direct other-user profile reads
1) Find the direct read that fetches another user's profile (audit: eventsStore.js around line ~313).
2) Replace it with RPC:
   - If it needs one user: call get_profiles_by_ids with [userId] and take first row.
   - No direct supabase.from("profiles") reads for other users anywhere.

C) Replace ALL alert() calls with modal/toast
1) Replace alert() in:
   - src/components/challenges/ChallengeGame.jsx
   - src/components/events/EventQuiz.jsx
   - src/pages/CheckEmailScreen.jsx
   - src/components/PurchaseStreakFreezeModal.jsx
2) Use the existing modal/toast pattern already used in Friends:
   - useModalStore().showModal(MODAL_TYPES.ERROR, { message })
   - For success: MODAL_TYPES.SUCCESS if it exists, otherwise use INFO.
3) Remove any remaining alert() calls from the whole repo (search for "alert(").

D) Fix silent catch blocks
1) Fix the fully silent catches in:
   - friendChallengesStore.js (2 locations mentioned)
   - progressStore.js (1 location mentioned)
2) No empty catch {} allowed.
   - At minimum: console.error(err)
   - And set store state error to err.message if that store has error state.

E) Remove debug noise logs
1) Remove or gate console.log() spam in:
   - friendChallengesStore.js
   - challengeStore.js
Keep only console.error for actual failures.
Do NOT touch IQ_STREAK_TRACE logs in progressStore if they are intentionally kept.

F) Add retry circuit breaker (awaitingProfileSetup)
1) In useUserStore retryInit / init flow:
   - Add retryCount in state
   - Max retries = 5
   - On exceeding max, show a modal with a clear message:
     "Account setup is taking longer than expected. Please try again later or contact support."
   - Do not loop automatically, user must tap retry.

G) Remove dead variables in Friends.jsx
1) Remove unused userLevel variables in the 4 card components mentioned by audit.
2) Ensure LevelBadgeCompact gets xp only (as currently intended).

H) Final verification
1) Run a full repo search to confirm:
   - No alert(
   - No empty catch {}
   - No supabase.from("profiles") reads for OTHER users (only own row is allowed)
2) Ensure friends mutations still refresh correctly and Friends/Search/Leaderboard still work.

Return: list of files changed + a short bullet list of what you changed per file.
Do not add new features.
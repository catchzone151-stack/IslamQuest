Fix account deletion and enforce strict email confirmation in my Supabase project.
Everything must be fully wired to Supabase (Auth + RLS + Edge Functions + route protection).

-----------------------------------
1) ACCOUNT DELETION (Atomic + Secure)
-----------------------------------
Requirements:
- Delete authenticated user from auth.users using Supabase Admin API (service role key)
- Must be implemented via a secure Supabase Edge Function
- No client-side admin logic
- Must delete ALL related data:
  - profiles
  - friends
  - friend_requests
  - streaks
  - progress
  - leaderboard/stats
- Must not leave orphaned data
- Must handle failure safely (no partial deletes)
- Must log the user out after success
- Must return proper success/error responses
- Must work with RLS enabled

If needed, include SQL for:
- Foreign keys with ON DELETE CASCADE
- RLS-safe policies
- Any necessary triggers

-----------------------------------
2) STRICT EMAIL CONFIRMATION (Cannot Be Bypassed)
-----------------------------------
Requirements:
- Supabase email confirmation must be enabled in dashboard
- Prevent login access if email_confirmed_at is NULL
- Even if a session exists, user must NOT access home/dashboard if not confirmed
- Enforce check using:
    supabase.auth.getUser()
    user.email_confirmed_at
- If NULL:
    - Immediately sign out user
    - Redirect to /confirm-email
- Add resend confirmation email logic
- Add route guard / middleware protection for all protected routes
- Must work on app reload and session restore
- Must not rely only on UI-level checks

-----------------------------------
Generate:
-----------------------------------
1) Full Edge Function file (complete working code)
2) Full client-side integration code (route guard included)
3) Required SQL (tables, FKs, RLS, cascades if needed)
4) Required Supabase dashboard settings
5) Step-by-step setup instructions

Make it production-ready.
No partial snippets.
Everything must be fully wired to Supabase.
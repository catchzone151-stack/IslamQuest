We are modifying the existing auth + XP system for launch.

⚠️ STRICT RULES:

DO NOT rebuild auth.

DO NOT modify Supabase auth configuration.

DO NOT create new tables.

DO NOT add new columns.

DO NOT duplicate verification state.

Reuse existing verification modal and resend logic.

Make minimal, surgical edits only.

Keep UI styling unchanged.

🎯 OBJECTIVE

After signup:

User is automatically logged in.

Redirect to Home.

Show toast:
“Verification email sent. Unlock leaderboards & challenges once confirmed.”

Do NOT block app access.

Verification becomes a competitive integrity layer only.

🔐 SOURCE OF TRUTH

Use Supabase:

const isVerified = !!user?.email_confirmed_at

Do NOT create is_verified.
Do NOT store verification in local DB.

🧠 XP HARD CAP LOGIC

Modify the central XP update logic only.

If !isVerified:

Total XP must not exceed 1000.

If XP >= 1000 → prevent further increases.

Show modal:
“Verify your email to continue progressing.”

Do NOT scatter cap logic across components.
Keep it in one XP mutation function.

If verified:

Remove cap immediately.

Keep current XP as-is.

No retroactive adjustments.

👥 FRIENDS & COMPETITION GATING

Unverified users:

Can enter Friends page.

Clicking any of:

Add Friend

Leaderboard

Requests

Search

Send Challenge

Accept Challenge

→ Open existing verification modal.

Do NOT grey out UI.
Do NOT redesign layout.

🏆 LEADERBOARD PROTECTION

Before any leaderboard write/update:

Check isVerified

If false → block and open verify modal

Unverified users must:

Never appear in leaderboard queries.

Never write leaderboard data.

Filter leaderboard queries to:
Only include users where email_confirmed_at IS NOT NULL.

🔄 SESSION REFRESH

On:

App load

Window focus

Manual refresh button in verify modal

Re-fetch session and re-evaluate email_confirmed_at.

If verified:

Unlock all features instantly.

Remove XP cap.

No logout required.

🛡 INTEGRITY GUARDS

Add verification check before:

addFriend()

sendFriendRequest()

acceptRequest()

sendChallenge()

leaderboard submission

Simple guard:

If not verified → open verify modal → return early.

🚫 DO NOT

Change Supabase dashboard config

Create new state managers

Refactor routing

Touch database schema

Introduce background jobs

This is a launch-ready integrity layer.